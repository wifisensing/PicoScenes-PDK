name: plugin Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ${{ matrix.runner }}

    strategy:
      matrix:
        runner: [ 'Linux22' ]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build
        run: |
          echo "Building...."

  deploy:
    runs-on: Linux22

    steps:
      - name: Deploy
        run: |
          curl -H "Authorization: token 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" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/wifisensing/PicoScenes-Driver/actions/workflows/main.yml/dispatches

