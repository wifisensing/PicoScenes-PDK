name: plugin Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ${{ matrix.runner }}

    strategy:
      matrix:
        runner: [ 'Linux22' ]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build
        run: |
          echo "Building...."

  deploy:
    runs-on: Linux22

    steps:
      - name: Deploy
        run: |
          curl -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: maoxiaodao9 ${{ secrets.DEPLOY_TOKEN }}" \
          https://api.github.com/repos/wifisensing/PicoScenes/actions/workflows/main.yml/dispatches \
          -d '{"event_type":"trigger-event"}'

